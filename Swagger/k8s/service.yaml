apiVersion: v1
kind: Service
metadata:
  name: cinema-api-service
  namespace: api-governance
  labels:
    app: cinema-api-service
spec:
  type: ClusterIP
  selector:
    app: cinema-api-service
  ports:
    - name: http
      port: 80
      targetPort: http
