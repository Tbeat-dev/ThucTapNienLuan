apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cinema-api-service
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  lifecycle: experimental
  owner: group:guests
  providesApis:
    - cinema-movie-api   # phải khớp chính xác metadata.name của entity API bên dưới
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: cinema-movie-api
  description: OpenAPI contract for managing cinema movies.
  tags:
    - openapi
    - governance
spec:
  type: openapi
  lifecycle: experimental
  owner: group:guests
  definition:
    $text: ./swagger.json